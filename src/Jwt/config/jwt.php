<?php

return [
    'default' => 'default',

    'modality' => [
        'default' => [
            /*
            |--------------------------------------------------------------------------
            | 签发者
            |--------------------------------------------------------------------------
            |
            | 签发该 jwt 的应用程序
            |
            */

            'iss' => env('APP_URL', 'http://localhost'),

            /*
            |--------------------------------------------------------------------------
            | 所面向的用户
            |--------------------------------------------------------------------------
            |
            | 该 jwt 所可以使用的应用程序
            |
            */

            'sub' => env('APP_NAME', 'Laravel'),

            /*
            |--------------------------------------------------------------------------
            | 接收的一方
            |--------------------------------------------------------------------------
            |
            | 可使用该 jwt 的访问来源
            |
            */

            'aud' => '*',

            /*
            |--------------------------------------------------------------------------
            | 加密用到的字符串
            |--------------------------------------------------------------------------
            |
            | 该 jwt 加密所使用的密钥
            |
            */

            'key' => env('APP_KEY'),

            /*
            |--------------------------------------------------------------------------
            | 加密方式
            |--------------------------------------------------------------------------
            |
            | 该 jwt 使用密钥加密的方式
            |
            */

            'alg' => 'HS256',

            /*
            |--------------------------------------------------------------------------
            | 生效时间
            |--------------------------------------------------------------------------
            |
            | 该 jwt 签发后在当前时间多少秒后开始生效
            |
            */

            'nbf' => 0,

            /*
            |--------------------------------------------------------------------------
            | 过期时间
            |--------------------------------------------------------------------------
            |
            | 该 jwt 在生效后多少秒后过期
            |
            */

            'exp' => 7200,
        ]
    ]
];
